FE/
│
├── pages/            // chứa các trang riêng (home, about, project...)
│
├── assets/           // tài nguyên tĩnh
│   ├── css/          // toàn bộ file CSS
│   │   └── pages/    // CSS riêng cho từng trang
│   ├── images/       // ảnh
│   ├── icons/        // icon
│   └── fonts/        // font chữ
│
├── components/       // các phần tái sử dụng (header, footer, navbar...)
│
├── data/             // dữ liệu giả (mock) nếu cần hiển thị portfolio
│
├── scripts/          // để trống hoặc dùng sau nếu thêm JS
│
└── docs/             // ghi chú, mô tả project (CV, mô tả dự án...)